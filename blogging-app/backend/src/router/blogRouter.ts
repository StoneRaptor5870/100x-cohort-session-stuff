import { Hono } from "hono";

export const blogRouter = new Hono();

// blogRouter.get('/allBlogs', getPosts);
// blogRouter.get('/userBlogs', getUserPosts);
// blogRouter.post('/createBlog', createPost);
// blogRouter.get('/blog/:id', getPost);
// blogRouter.put('/blog/:id', updatePost);
// blogRouter.delete('/blog/:id', deletePost);